// A variable can be easily defined by assign to it
a = 42;
// A variable can not be changed if not defined mutable first
mut b = 1234;
// A variable can only be reassigned with the same type
b = 56;
//- b = vec2(1, 1) // Error!

a * b