// A function can be easily defined by assigning to it
fn a() = 42;
// A function can never be changed after definition!
//- fn a() = 1234; // Error!
// A function can have generic parameters
fn a(x) = x * 42;
// A function can also specifiy the type of the parameter
fn a(x:int) = x * 111;
// A function can be overloaded. It will trigger an error if the exact same signature already exists
fn a(x:num, y:num) = x * y;
//- fn a(x:int) = x * 42; // Error!

a(111)